Transofrm in primary next to primary.next().value
def collatz_generator(n):
    while n != 1:
        yield n
        n = n // 2 if n % 2 == 0 else 3 * n + 1
    yield 1

ADd builtin func ord 

Fix += -= *= after alterations to + , - , *
Print in python is different than javascript make a function for print that takes tthat into consideration
add function in window so it is builtin see how you do it in the new beowser I defnin

change name of funcs that are builtin when called inside another function map(sum, [1,2,3])
lst = [z for x, y in [(2, 3), (4, 5)] for z in (x, y)]
print(lst)  # [2, 3, 4, 5]

Strings builtin funcs
pairs = [(x, y) for x in range(2) for y in range(3)]
print(pairs)  # [(0, 0), (0, 1), (0, 2), (1, 0), (1, 1), (1, 2)]

genexp - next , list : implementar uma própria static function ?

wildcard problem _ add variables wildcard_osiris_1 , ... 

notas = list(map(float, input("Digite as notas separadas por espaço: ").split()))

def juros_compostos_lista(valores):
    p, r, t = valores
    return p * (1 + r) ** t

valores = [1000, 0.05, 3]
print(juros_compostos_lista(valores))





