Find a way on evalComparison to not pass that context so explicitly because the code becomes really hard to redad -> TO DO THIS ADD STATIC CONTEXT THE BUILTIN FUNC AND PASS ONLY THE OTHER STUFF ADD CODE FOR THIS
Teste de hipótese

Adicionar escrita

def eh_primo(n):
    if n < 2:
        return "Não é primo"
    divisores = [i for i in range(2, n) if n % i == 0]
    return "Não é primo" if divisores else "É primo"

Transofrm in primary next to primary.next().value
def collatz_generator(n):
    while n != 1:
        yield n
        n = n // 2 if n % 2 == 0 else 3 * n + 1
    yield 1

Print in python is different than javascript make a function for print that takes tthat into consideration

Create input function 

comprehension generators dont work properly for more than one for 

notas = list(map(float, input("Digite as notas separadas por espaço: ").split()))
